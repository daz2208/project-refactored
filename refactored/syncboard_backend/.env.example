# =============================================================================
# SyncBoard 3.0 Knowledge Bank - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit the .env file to version control!
#
# Generate secure keys with: openssl rand -hex 32
# =============================================================================

# REQUIRED: Secret key for JWT token signing
# Generate with: openssl rand -hex 32
SYNCBOARD_SECRET_KEY=your-secret-key-here-change-this

# REQUIRED: OpenAI API key for AI features
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# SECURITY: Allowed CORS origins (comma-separated)
# Default: * (allows all - INSECURE for production!)
# Production example: http://localhost:3000,https://yourdomain.com
SYNCBOARD_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# OPTIONAL: Storage configuration
SYNCBOARD_STORAGE_PATH=storage.json
SYNCBOARD_VECTOR_DIM=256

# OPTIONAL: Authentication settings
SYNCBOARD_TOKEN_EXPIRE_MINUTES=1440

# OPTIONAL: Tesseract OCR path (Windows only)
# TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe
